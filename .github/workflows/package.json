{
  "name": "CIVIC",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'Running tests...' && exit 0",  // Replace with actual test command
    "build": "echo 'Building project...' && exit 0"  // Replace with actual build command
  },
  "dependencies": {
    // List your dependencies here
  }
}
